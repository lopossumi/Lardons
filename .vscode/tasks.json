// add build task that runs "bash build.sh"

{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "bash build.sh"
        }
    ]
}